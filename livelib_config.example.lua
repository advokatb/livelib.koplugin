-- Copy this file to livelib_config.lua and fill in your credentials.
-- Cookies: browser DevTools (F12 → Application → Cookies → livelib.ru)
-- Copy ALL domain cookies in one line: name1=value1; name2=value2; ...
return {
  session_cookie = '',
}
